=begin

If I see the following error message:

NoMethodError: undefined method `keys' for Array

I suspect that there's no method called `keys` for Array objects.
The `keys` method is a method for Hash objects.

=end